<UserControl x:Class="RMS.WPF.Settings.StorageSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:settings="clr-namespace:RMS.WPF.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=settings:StorageSettingsViewModel}">

    <Border Style="{StaticResource CardBorderStyle}" MaxWidth="900">
        <StackPanel>
            <TextBlock Text="Configure where files are stored. Missing folders are created automatically when you save."
                       FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,8"/>

            <ItemsControl ItemsSource="{Binding Folders}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <settings:FolderSettingRowControl />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
