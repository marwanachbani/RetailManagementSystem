<Window x:Class="RMS.WPF.Printing.BarcodeLabelDesignerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:printing="clr-namespace:RMS.WPF.Printing"
        mc:Ignorable="d"
        Title="Barcode Label Designer"
        Height="520" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Barcode Data" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="DataBox" Text="6000123456789" Style="{StaticResource ModernTextBoxStyle}" TextChanged="OnChanged"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Symbology" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="SymbologyBox" Style="{StaticResource ModernComboBoxStyle}" SelectionChanged="OnChanged"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Product Name" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="NameBox" Text="Sample Product" Style="{StaticResource ModernTextBoxStyle}" TextChanged="OnChanged"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Price" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            <TextBox Grid.Row="3" Grid.Column="1" x:Name="PriceBox" Text="$1.99" Style="{StaticResource ModernTextBoxStyle}" TextChanged="OnChanged"/>
        </Grid>

        <Border Grid.Row="4" Background="White" Margin="0,12" Padding="12" HorizontalAlignment="Center">
            <Image x:Name="PreviewImage" Stretch="None" MaxWidth="420"/>
        </Border>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Print Label" x:Name="PrintButton" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" Click="PrintButton_Click"/>
            <Button Content="Close" x:Name="CloseButton" Style="{StaticResource SecondaryButtonStyle}" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
