<Window x:Class="RMS.WPF.Printing.PrintPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Print Preview"
        Height="720" Width="640"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="12,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Zoom" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0"/>
                <Slider x:Name="ZoomSlider" Minimum="50" Maximum="200" Value="100" Width="160" VerticalAlignment="Center"/>
                <TextBlock x:Name="ZoomLabel" Text="100%" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" Margin="8,0,0,0" Width="40"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Background="Gray">
            <Border x:Name="PageHost" Background="White" Margin="16">
                <WebBrowser x:Name="Browser"/>
            </Border>
        </ScrollViewer>

        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="12,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Print" x:Name="PrintButton" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" Click="PrintButton_Click"/>
                <Button Content="Export PDF" x:Name="ExportButton" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Click="ExportButton_Click"/>
                <Button Content="Save" x:Name="SaveButton" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0" Click="SaveButton_Click"/>
                <Button Content="Close" x:Name="CloseButton" Style="{StaticResource SecondaryButtonStyle}" Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
