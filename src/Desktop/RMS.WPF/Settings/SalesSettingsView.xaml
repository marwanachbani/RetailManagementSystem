<UserControl x:Class="RMS.WPF.Settings.SalesSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:settings="clr-namespace:RMS.WPF.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=settings:SalesSettingsViewModel}">

    <Border Style="{StaticResource CardBorderStyle}" MaxWidth="760">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Default Tax Rate (%)" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding DefaultTaxRate, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Width="120" HorizontalAlignment="Left"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Default Discount (%)" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DefaultDiscount, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Width="120" HorizontalAlignment="Left"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Maximum Discount (%)" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding MaximumDiscount, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Width="120" HorizontalAlignment="Left"/>

                <Label Grid.Row="3" Grid.Column="0" Content="Default Payment Method" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding PaymentMethods}" SelectedItem="{Binding DefaultPaymentMethod}" Style="{StaticResource ModernComboBoxStyle}" Width="200" HorizontalAlignment="Left"/>

                <CheckBox Grid.Row="4" Grid.Column="1" Content="Allow Manual Price Change" IsChecked="{Binding AllowManualPriceChange}" Margin="0,6"/>
                <CheckBox Grid.Row="5" Grid.Column="1" Content="Require Manager Approval" IsChecked="{Binding RequireManagerApproval}" Margin="0,6"/>
                <CheckBox Grid.Row="6" Grid.Column="1" Content="Auto Complete Sale" IsChecked="{Binding AutoCompleteSale}" Margin="0,6"/>
                <CheckBox Grid.Row="7" Grid.Column="1" Content="Receipt Preview" IsChecked="{Binding ReceiptPreview}" Margin="0,6"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
