<UserControl x:Class="RMS.WPF.Settings.PrinterSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:settings="clr-namespace:RMS.WPF.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=settings:PrinterSettingsViewModel}">

    <Border Style="{StaticResource CardBorderStyle}" MaxWidth="760">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Default Printer" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Printers}" IsEditable="True" Text="{Binding DefaultPrinter, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Receipt Printer" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Printers}" IsEditable="True" Text="{Binding ReceiptPrinter, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Invoice Printer" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Printers}" IsEditable="True" Text="{Binding InvoicePrinter, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}"/>

                <Label Grid.Row="3" Grid.Column="0" Content="Label Printer" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Printers}" IsEditable="True" Text="{Binding LabelPrinter, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}"/>

                <Label Grid.Row="4" Grid.Column="0" Content="Report Printer" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding Printers}" IsEditable="True" Text="{Binding ReportPrinter, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernComboBoxStyle}"/>

                <CheckBox Grid.Row="5" Grid.Column="1" Content="Automatic Printing" IsChecked="{Binding AutoPrint}" Margin="0,6"/>
                <CheckBox Grid.Row="6" Grid.Column="1" Content="Cut Paper (Thermal)" IsChecked="{Binding CutPaper}" Margin="0,6"/>
                <CheckBox Grid.Row="7" Grid.Column="1" Content="Open Cash Drawer (Thermal)" IsChecked="{Binding OpenDrawer}" Margin="0,6"/>

                <Label Grid.Row="8" Grid.Column="0" Content="Copies" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding Copies, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Width="80" HorizontalAlignment="Left"/>

                <Label Grid.Row="9" Grid.Column="0" Content="Paper Width (mm)" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding PaperWidth, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Width="80" HorizontalAlignment="Left"/>

                <Label Grid.Row="10" Grid.Column="0" Content="Margins (mm)" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBox Grid.Row="10" Grid.Column="1" Text="{Binding MarginMm, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Width="80" HorizontalAlignment="Left"/>

                <Label Grid.Row="11" Grid.Column="0" Content="Orientation" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <ComboBox Grid.Row="11" Grid.Column="1" ItemsSource="{Binding Orientations}" SelectedItem="{Binding Orientation}" Style="{StaticResource ModernComboBoxStyle}" Width="160" HorizontalAlignment="Left"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
