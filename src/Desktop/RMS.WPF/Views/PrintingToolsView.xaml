<UserControl x:Class="RMS.WPF.Views.PrintingToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:RMS.WPF.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PrintingToolsViewModel}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8">
        <StackPanel MaxWidth="760">
            <TextBlock Text="Printing &amp; Document Management" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="Generate, preview and print every document. Configure printers in Settings." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,12"/>

            <Border Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Test Prints" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                    <WrapPanel Orientation="Horizontal">
                        <Button Content="Print Test Receipt" Command="{Binding TestReceiptCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,8"/>
                        <Button Content="Print Test Invoice" Command="{Binding TestInvoiceCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,8"/>
                        <Button Content="Print Test Label" Command="{Binding TestLabelCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,8"/>
                        <Button Content="Print Test Barcode" Command="{Binding TestBarcodeCommand}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,8"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,12,0,0">
                <StackPanel>
                    <TextBlock Text="Tools" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                    <WrapPanel Orientation="Horizontal">
                        <Button Content="Barcode Label Designer" Command="{Binding OpenDesignerCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,8"/>
                        <Button Content="Select Printer" Command="{Binding SelectPrinterCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,8"/>
                        <Button Content="Refresh Printers" Command="{Binding RefreshPrintersCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,8"/>
                    </WrapPanel>

                    <TextBlock Text="Installed printers:" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,8,0,4"/>
                    <ListBox ItemsSource="{Binding Printers}" Height="120" BorderBrush="{StaticResource BorderBrush}"/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,12,0,0">
                <TextBlock Text="{Binding Status}" Foreground="{StaticResource TextSecondaryBrush}"/>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
