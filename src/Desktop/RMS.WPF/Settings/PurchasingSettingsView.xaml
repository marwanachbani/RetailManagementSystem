<UserControl x:Class="RMS.WPF.Settings.PurchasingSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:settings="clr-namespace:RMS.WPF.Settings"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=settings:PurchasingSettingsViewModel}">

    <Border Style="{StaticResource CardBorderStyle}" MaxWidth="760">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Default Supplier" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding DefaultSupplier, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Purchase Number Prefix" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PurchaseNumberPrefix, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBoxStyle}" Width="160" HorizontalAlignment="Left"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Default Payment Terms" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding PaymentTermsOptions}" SelectedItem="{Binding DefaultPaymentTerms}" Style="{StaticResource ModernComboBoxStyle}" Width="200" HorizontalAlignment="Left"/>

                <CheckBox Grid.Row="3" Grid.Column="1" Content="Automatic Goods Receipt" IsChecked="{Binding AutomaticGoodsReceipt}" Margin="0,6"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
