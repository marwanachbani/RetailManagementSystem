<Window x:Class="RMS.WPF.Views.CreateSaleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="New Sale"
        Height="650"
        Width="800"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="New Sale" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Add Item -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
            <TextBox Grid.Column="1" Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="60" Margin="0,0,10,0"/>
            <TextBox Grid.Column="2" Text="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="80" Margin="0,0,10,0"/>
            <Button Grid.Column="3" Content="Add" Width="60" Command="{Binding AddItemCommand}"/>
        </Grid>

        <!-- Items -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" />
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" />
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=C}" />
                <DataGridTextColumn Header="Total" Binding="{Binding TotalPrice, StringFormat=C}" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Totals -->
        <Grid Grid.Row="3" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                <TextBlock Text="{Binding SubTotal, StringFormat='SubTotal: {0:C}'}" />
                <TextBlock Text="{Binding DiscountAmount, StringFormat='Discount: {0:C}'}" />
                <TextBlock Text="{Binding TaxAmount, StringFormat='Tax: {0:C}'}" />
                <TextBlock Text="{Binding TotalAmount, StringFormat='Total: {0:C}'}" FontWeight="Bold" FontSize="16"/>
            </StackPanel>
        </Grid>

        <!-- Discount / Tax -->
        <Grid Grid.Row="4" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Discount %:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Grid.Column="1" Text="{Binding DiscountPercentage, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,20,0"/>
            <TextBlock Grid.Column="2" Text="Tax %:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Grid.Column="3" Text="{Binding TaxPercentage, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <!-- Actions -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="{Binding StatusMessage}" Foreground="Red" Margin="0,0,20,0" VerticalAlignment="Center"/>
            <Button Content="Remove Item" Command="{Binding RemoveItemCommand}" Margin="0,0,10,0"/>
            <Button Content="Complete Sale" Command="{Binding CompleteSaleCommand}" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
