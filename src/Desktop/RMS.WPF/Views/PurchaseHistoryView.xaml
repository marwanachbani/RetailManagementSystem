<UserControl x:Class="RMS.WPF.Views.PurchaseHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Grid.Column="0" Margin="0,0,8,0" Style="{StaticResource ModernTextBoxStyle}"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding StatusFilters}" SelectedIndex="{Binding SelectedStatusFilter, Mode=TwoWay}" Width="140" Margin="0,0,8,0"/>
            <Button Content="Search" Command="{Binding SearchCommand}" Grid.Column="2" Style="{StaticResource SecondaryButtonStyle}"/>
        </Grid>

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,12" FontSize="12"/>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding PurchaseOrders}" SelectedItem="{Binding SelectedOrder}" AutoGenerateColumns="False" IsReadOnly="True" Style="{StaticResource ModernDataGridStyle}" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PO Number" Binding="{Binding PurchaseNumber}" Width="140"/>
                    <DataGridTextColumn Header="Supplier" Binding="{Binding SupplierName}" Width="180"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding OrderDate, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="160"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="130"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=C}" Width="110"/>
                    <DataGridTextColumn Header="Invoice" Binding="{Binding SupplierInvoiceNumber}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
